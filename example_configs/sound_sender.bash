# $confdir — папка, содержащая этот файл

# адрес сервера распознавания (диспетчерв)
server=192.168.1.92

# Файл с паролем к серверу. Его имя, без расширения .pw — имя пользователя
pw_file="$confdir/nuc.pw"

# Рабочая папка — откуда брать файлы
workdir="$HOME/talks"
